# Task: Hapus semua commit (Delete all commits)

## Completed:
- [x] Removed .git directory (all commits/history deleted)
- [ ] git init (fresh repo)
- [ ] git add . && git commit -m "Initial commit"
- [ ] git branch -M main
- [ ] git remote add origin https://github.com/Zacky171/story-map-spa-personal.git
- [ ] git push -u origin main --force (overwrite remote)

## Notes:
- All source files preserved.
- Run `git status` to verify no repo.
- Force push will delete remote history too.
